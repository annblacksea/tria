export const selectUserRole = (state) => state.user.userData.roleId;

// export const selectUserRole = (state) => {
//   console.log('DEBUG STORE STATE:', state.user);
//   return state.user?.userData?.roleId;
// };
